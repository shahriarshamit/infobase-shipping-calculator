on: push
name: Deploy website on push
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v3
    
    - name: Sync files
      uses: shahriarshamit/infobase-shipping-calculator@latest
      with:
        server: 66.45.229.178
        username: admin@devstudio.cu.ma
        password: ${{ secrets.ftp_password }}
